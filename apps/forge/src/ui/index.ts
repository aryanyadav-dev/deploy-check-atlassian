/**
 * UI Components Index
 *
 * Exports all UI components for the Forge app.
 */

// Badges
export { RiskBadge, CompactRiskBadge, RiskBadgeWithScore } from './badges';

// Modals
export { SolutionDetailModal } from './modals';

// Panels are exported as render functions, not components
// They are registered in the manifest and invoked by Forge
